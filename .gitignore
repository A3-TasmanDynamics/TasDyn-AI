node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# SQLite data files — these hold real member/ticket data and must never be committed.
# See docs/SECURITY-DATABASE.md: history predating this .gitignore entry still needs scrubbing.
data/
*.db
*.db-shm
*.db-wal

