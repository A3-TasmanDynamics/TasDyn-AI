node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# SQLite data files and audit logs — these hold real member/ticket/moderation data and must
# never be committed. See docs/SECURITY-DATABASE.md: history predating this .gitignore entry
# still needs scrubbing.
data/
*.db
*.db-shm
*.db-wal
audit_logs/

